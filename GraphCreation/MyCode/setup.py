from setuptools import setup, find_packages

setup(
    name="ts2g2",
    version="0.1.0",
    packages=find_packages(),  # automatically finds ts2g2/ and submodules
    install_requires=[],       # optionally add dependencies here
)